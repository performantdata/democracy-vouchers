package com.performantdata.voucher

/** Types exposed by the GraphQL schema.
  * 
  * These are distinct from the database types so that they present a stable client API
  * in the face of database schema changes.
  */
package object schema {}
